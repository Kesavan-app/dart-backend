name: backend
description: Dart backend API for Railway
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  mysql1: ^0.20.0
  bcrypt: ^1.1.3
  crypto: ^3.0.7
  dart_jsonwebtoken: ^3.3.1
  http: ^1.6.0
